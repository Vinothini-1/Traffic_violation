import React from 'react';
import ReactDOM from 'react-dom';
import "./pages/Detection.scss";
import PaymentInfo from './pages/PaymentInfo.jsx';

ReactDOM.render(
    <React.StrictMode>
        <PaymentInfo/>
    </React.StrictMode>,
    document.getElementById('root')
);
